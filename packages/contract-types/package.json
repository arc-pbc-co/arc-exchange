{
  "name": "@arc-exchange/contract-types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "echo 'Run from contracts package after compilation'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "viem": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
